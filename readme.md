-   useNavigate(), как и useDispatch() возвращают функции. Поэтому нужно их изначально отдельно в функциях вызывать.
    const navigate = useNavigate();

Этот хук используется для изменения адресной строки
